# 更新日志

## 2026-09-25：宿主载荷 v1.3.3、统一安装器 v0.1.10

- 战斗和水果关读取词表前，核对存档书名、原生槽位指纹与词书中心归属，恢复当前受管词书；战斗列表缓存逐词校验，避免同长度旧列表残留。
- 小游戏和普通单词按钮共用当前词书音频路由。受管词缺少包内音频时阻止英语回退并记录日志；未知文本维持游戏原有处理。新安装默认关闭向共用 `vocabulary` 目录镜像音频，旧配置可自行保留开关设置。
- 当前词书词形建立集合索引，减少发音请求的线性扫描。安装器目录指向 `wcp-mods-v1.3.3`。

离线验证：宿主编译、注册表、接管范围和音频测试通过；ZIP 内容及 SHA-256 回读通过。法语战斗、水果关、切书和第三方词书换出换回仍待实机测试。全局已学词记录无词书身份，同形词仍可能影响排序。

## 2026-09-23：宿主载荷 v1.3.2、统一安装器 v0.1.9

- 修复战斗出题前宿主未识别已保存词书的情况：只有存档词书、原生槽位和资源清单指纹一致时，才恢复当前词表并重建战斗队列。
- 离开词书接管范围时，若旧战斗队列含当前词书以外的词，不再把这份队列恢复进游戏内存。
- 修复位于九种受管语言共用的宿主逻辑；不按拼写把英法同形词误判为跨语言混入。
- 安装器内置目录已更新为 `wcp-mods-v1.3.2`，启动器版本及自更新索引升至 `wcp-installer-v0.1.9`。下载与解压进度显示沿用 v0.1.8。

验证：宿主注册表、接管范围、安装器、自更新及双击入口离线回归通过；载荷与安装器 ZIP 回读无损坏，包内 DLL、目录版本和索引 SHA-256 与源文件一致。逐语种实机战斗与切换仍需现场复测；本次没有修改发音音频。

## 2026-09-23：统一安装器 v0.1.8

- 下载 mod、词书和自更新包，以及展开 ZIP 时显示百分比和已处理字节数；完成时显示 `100%`。
- Windows PowerShell 5.1 离线回归：安装器 118/118、自更新 10/10、双击入口 5/5。
